package com.dl.one;

public enum Seasons {
	
	SUMMER,
	AUTUMN,
	WINTER,
	RAINY;

}

















//Keypoint

//Replace the class with enum
//separate the constants with comma(,) use capital letters to define constants
//Enums are used to represent set of name constants 
//Fixed constants are  never changes like , months, weeks, seasons
//Constants declared in enum should be capital letters
